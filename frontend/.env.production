# Leave VITE_API_BASE_URL unset in Vercel. Production uses same-origin /api (see frontend/api proxy).
# Optional override for local/staging builds only:
# VITE_API_BASE_URL=https://your-service-xxxx.onrender.com
