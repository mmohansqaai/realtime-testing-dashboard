# Optional: set in Vercel → Environment Variables only if your Render URL differs from
# `DEFAULT_PROD_API` in `src/App.tsx` (otherwise omit — the build uses that constant).
# VITE_API_BASE_URL=https://your-service-xxxx.onrender.com
